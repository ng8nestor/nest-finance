// Plain data about the site, kept out of the components that render it.
// `src/lib/` is for non-visual code: constants, helpers, API clients — anything
// that isn't a React component. Pages import from here so the copy lives in one
// place instead of being retyped into JSX.

export const SITE_NAME = "nest";

export const SITE_TAGLINE = "Personal finance, quietly organized.";
